# Descriptor Validation

Future descriptor validation should confirm expected keyboard and relative mouse collections, report IDs, usages, and report lengths. Descriptor validation is read-only and must not include report writes.
